'use strict';
module.exports = {
  name: process.env.STORE_NAME || 'MNMUKT',
  logo: process.env.STORE_LOGO_URL,       // real hosted URL, not a placeholder
  themeColor: process.env.STORE_THEME_COLOR || '#000000',
};